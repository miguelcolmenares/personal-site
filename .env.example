# Canonical origin for Open Graph and canonical URLs.
# Unset, the site falls back to its Vercel hostname. Set this in the Vercel
# project when the custom domain is attached — no code change needed.
# NEXT_PUBLIC_SITE_URL=https://miguelcolmenares.com
